# Secrets / local env (never commit real values)
.env
.env.*
!.env.example
!.env.dev.example
!.env.prod.example

# Pyrogram sessions (phone / auth files)
sessions_parogram/

# Telegram group/channel exports (local)
data/telegram_exports/

# Local catalog course JSON
data/catalog/categories/

# Import payload with live bot tokens (keep example.json)
scripts/telegram_catalog.flancki.json

# Proxy pool credentials (host:port:user:pass)
data/catalog/proxies.json

# Ephemeral docker worker batch mounts
data/catalog/.worker_runs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.eggs/

# Databases (local SQLite)
*.db
*.sqlite3

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.vercel
.graphify