# Secrets / local env (never commit real values)
.env
.env.*
!.env.example
!.env.dev.example
!.env.prod.example

# Pyrogram sessions (phone / auth files)
sessions_parogram/

# Local data (catalog dumps, proxies, worker runs, telegram exports)
data/

# Import payload with live bot tokens (keep example.json)
scripts/telegram_catalog.flancki.json

# Bot dumps with tokens / personal Telegram data
scripts/bots/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.eggs/

# Databases (local SQLite)
*.db
*.sqlite3

# Tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.vercel
.graphify

# App / fetch logs (local files; Vercel uses /tmp)
logs/