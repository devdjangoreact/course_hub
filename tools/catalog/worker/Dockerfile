# STUB — next stage: SearXNG + Chrome + enrich worker (proxy kill-switch).
# Do NOT COPY publish/, post_channel.py, or Bot API deps into this image.
#
# Intended later:
#   - start SearXNG on localhost
#   - NO_PROXY=localhost,127.0.0.1
#   - require HTTP_PROXY/HTTPS_PROXY/ALL_PROXY (fail closed)
#   - run enrich batch I/O contract → tools.catalog.enrich_searxng
#
# Not implemented in this stage.

FROM scratch
LABEL org.opencontainers.image.description="catalog enrich worker stub — not buildable yet"
